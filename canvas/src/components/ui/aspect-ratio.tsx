//ensures child content always maintain a fixed aspect ratio no matter the screen size or container

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

//alias the main component for easier use
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }

